#include "main.h"

/**
 *
 * _isupper - checks if a letter is uppercase
 * @u: number being checked
 * Return: 1 for uppercase, else 0
 */

int _isupper(int u)

{
	if (u >= 65 && u <= 90)
	{
		return (1);
	}
	return (0);
}	
