#include "main.h"

/**
 * _abs - computes absolute value of a integer
 * @w: the int to check
 * Return: the absolute value of int
 */

int _abs(int w)
{
	if (w >= 0)
	{
		return (w);
	}
	return (-w);
}
